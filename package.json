{
  "name": "arc-cross-border-payments",
  "version": "1.0.0",
  "description": "Cross-border payments and remittance platform on Arc testnet",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network arc",
    "deploy:testnet": "hardhat run scripts/deploy.js --network arcTestnet",
    "test": "hardhat test",
    "clean": "hardhat clean"
  },
  "keywords": [
    "arc",
    "blockchain",
    "cross-border",
    "payments",
    "remittance",
    "solidity"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-chai-matchers": "^2.0.0",
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "hardhat": "^2.22.0",
    "ethers": "^6.10.0",
    "dotenv": "^16.3.1",
    "chai": "^4.3.10"
  },
  "dependencies": {}
}
